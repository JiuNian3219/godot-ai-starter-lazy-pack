Use the `godot-test` skill. Add or improve Godot 4.6 verification for the requested behavior. Prefer lightweight command-line tests first, update `scripts/verify.ps1`, and explain what the test protects.

